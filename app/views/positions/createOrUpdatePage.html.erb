<p id="notice"><%= notice %></p>

<%= form_for(@position, :url => "/createOrUpdatePosition.xml") do |f| %>
    <% if @position.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@position.errors.count, "error") %> prohibited this position from being saved:</h2>

          <ul>
            <% @position.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <label for="phoneID">phoneID</label><br />
      <input id="phoneID" name="phoneID" size="30" type="text" value="1">
    </div>
    <div class="field">
      <label for="mapID">mapID</label><br />
      <input id="mapID" name="mapID" size="30" type="text" value="1">
    </div>
    <div class="field">
      <label for="x">x</label><br />
      <input id="x" name="x" size="30" type="text" value="1">
    </div>
    <div class="field">
      <label for="y">y</label><br />
      <input id="y" name="y" size="30" type="text" value="1">
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>


<%= link_to 'Edit', edit_position_path(@position) %> |
<%= link_to 'Back', positions_path %>
